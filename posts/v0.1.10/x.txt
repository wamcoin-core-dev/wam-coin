POST 1 — the release. 258 characters, counted not estimated.
============================================================

WAM v0.1.10: mine against your own node, with no pool at all.

wam-miner --solo -u YOUR_ADDRESS

Find a block, keep all 47.5 WAM.

Linux, Windows and macOS — every build proves the miner builds a valid
block on all three before it ships.

gitlab.com/WAMCoin/wam-coin


POST 2 — a day later. The three homes. 265 characters, counted.
===============================================================

WAM's source lives in three places, at three companies:

gitlab.com/WAMCoin/wam-coin
github.com/wamcoin-core-dev/wam-coin
gitea.com/WAMCoin/wam-coin

One repository, pushed to all three. If one is unreachable, the other two
are — and you don't need us to say which.


NOTES
=====

Post 1 alone if you post only one. It is the release and the invitation in the
same breath, and it needs no second message to make sense.

Do not merge the two. A release announcement that also explains where the code
moved reads as a project with a problem rather than one with a spare, and the
three homes are worth their own post on the day after the release.

No hash rate figure and no block height in either. Both move, an X post does
not, and a stale number is worse than no number on the one platform where the
post outlives its own facts.
